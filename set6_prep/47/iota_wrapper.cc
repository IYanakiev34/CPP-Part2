#include "iota_wrapper.h"

IotaWrapper::IotaWrapper(unsigned const &start, IotaFunc const &func) : d_Value(start),
                                                                        d_Func(func)
{
}